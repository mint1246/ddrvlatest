 ______   ______   _______  ____   ____
|_   _ `.|_   _ `.|_   __ \|_  _| |_  _|
  | | `. \ | | `. \ | |__) | \ \   / /
  | |  | | | |  | | |  __ /   \ \ / /
 _| |_.' /_| |_.' /_| |  \ \_  \ ' /
|______.'|______.'|____| |___|  \_/
=========================================
Not-so-conventional tool to exploit Discord's unlimited file attachment feature for an endless cloud storage solution.

HIGHLIGHTS
----------
- Async Rust implementation using tokio + reqwest + axum + libunftp.
- Supports HTTP and FTP frontends.
- Metadata backends: redb (embedded) and PostgreSQL.
- HTTP supports partial downloads (Range requests).
- HTTPS must be terminated by a reverse proxy or load balancer; the built-in
  frontend serves plain HTTP only. TLS configuration keys are rejected so that
  a configuration cannot silently claim to enable HTTPS.

REQUIREMENTS
------------
- Discord token(s)
- Optional PostgreSQL instance when using postgres backend

USAGE
-----
- Build: `cargo build --release`
- Run: `./target/release/ddrv --config=config.yaml`
- Migrate old master BoltDB to rewrite redb with main binary:
  `./target/release/ddrv --migrate /path/to/legacy.db --migrate-output /path/to/new.redb`

DISCORD CDN CORS WORKER
-----------------------
- Worker source: `cloudflare-worker/index.js` (config in `cloudflare-worker/wrangler.toml`).
- Deploy with `wrangler publish` from that directory.
- Configure ddrv to emit worker-backed URLs by setting `frontend.http.cdn_proxy_base` (or env `CDN_PROXY_BASE`, e.g. `https://your-worker.workers.dev/`).
- Client usage: `https://<your-worker>.workers.dev/?url=https://cdn.discordapp.com/attachments/...` (manifest/download URLs include `download=1` automatically).

USAGE DOCKER
------------
- Build image: `docker build -t ddrv:latest .`
- Run: `docker run -p 2525:2525 -p 2526:2526 -v "$PWD/config.yaml:/app/config.yaml" --rm ddrv:latest --config=/app/config.yaml`

BUILD FROM SOURCE
-----------------
- Clone this repository
- `cargo build --release`

CONFIGURATION FILE
------------------
- See `config/config_example.yaml` for a full sample.
- `frontend.http.addr` configures the plain HTTP listener. To expose HTTPS,
  terminate TLS in a reverse proxy (for example, Caddy or nginx) and proxy to
  that address. The unsupported `https_addr`, `https_keypath`, and
  `https_crtpath` settings (and their former `HTTPS_ADDR`, `HTTPS_KEYPATH`, and
  `HTTPS_CRTPATH` environment variables) are rejected during configuration loading.
- ddrv automatically searches for:
  - `./config.yaml`
  - `$HOME/.config/ddrv/config.yaml`
- Or pass explicit path: `ddrv --config=/path/to/config.yaml`
